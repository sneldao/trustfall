# dependencies
node_modules/
.svelte-kit/
build/

# wrangler/oauth state (contains credentials — never commit)
.config/
.wrangler/

# environment variables
.env
.env.*
!.env.example

# private docs — anything under private/ or *.private.* stays local
private/
*.private.*
PRIVATE*

# logs
*.log

# OS files
.DS_Store
._*
Thumbs.db

# editor
.vscode/
.idea/

# misc
*.local.*
*.tmp
*.swp
.cache/

# video scratch — render cache + source clips (not app assets;
# the clips the site ships live in static/clips/)
.remotion/
assets/*.mp4
assets/*.mov
assets/*.webm
