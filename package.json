{
  "name": "trustfall-hub",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "deploy": "wrangler pages deploy --project-name=trustfall-hub",
    "format": "prettier . --write",
    "prepare": "husky"
  },
  "devDependencies": {
    "@secretlint/secretlint-rule-preset-recommend": "^13.0.2",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.3",
    "prettier": "3.6.2",
    "secretlint": "^13.0.2"
  },
  "lint-staged": {
    "**/*": [
      "prettier --write --ignore-unknown"
    ]
  }
}
